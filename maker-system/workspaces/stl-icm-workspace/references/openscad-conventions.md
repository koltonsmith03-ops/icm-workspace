# OpenSCAD Conventions for this Workspace

- Prefer modules for major features.
- Use clear variable names.
- Comment the purpose of each major section.
- Avoid magic numbers when a named variable makes the intent clearer.
- Keep the top-level model simple so a human can open the file and understand it quickly.
- Do not add `$fn` or high-resolution settings unless the user asks for smoother curves.
